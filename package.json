{
  "name": "svelte-rickandmorty",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
     // "start": "react-scripts start",
    "start": "vite",
    // "build": "react-scripts build",
    "build": "tsc && vite build",
    "serve": "vite preview", // Añadimos este comando si deseamos tener una vista previa del bundle final
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^1.0.2",
    "svelte": "^3.49.0",
    "vite": "^3.1.0"
  }
}
